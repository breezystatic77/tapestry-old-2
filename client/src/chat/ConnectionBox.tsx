import React from 'react'

const ConnectionBox: React.FC<{
	characters: { [name: string]: string }
	connectAs: (name: string) => any
}> = ({ characters, connectAs }) => {
	return <div></div>
}

export default ConnectionBox
